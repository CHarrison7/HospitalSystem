spring:
  application:
    name: patientservice
  profiles:
    active: test
  #active: ${PROFILE}
  cloud:
    config:
      #uri: http://configserver:8888
      uri: http://localhost:8888
server:
  port: 8081

